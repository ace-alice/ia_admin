const toolPermission = {
  add: 'role:config:add',
}

const tablePermission = {
  is_disable: 'role:config:is_disable',
}

const otherPermission = {
  edit: 'role:config:edit',
  del: 'role:config:del',
  changeMenu: 'role:config:change:menu',
  saveMenu: 'role:config:saveMenu',
}

export { toolPermission, tablePermission, otherPermission }
