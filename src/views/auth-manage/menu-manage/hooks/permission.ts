const toolPermission = {
  add: 'menu:config:add',
}

const tablePermission = {
  // 是否可以需改需要权限验证
  is_un_auth: 'menu:config:un_auth',
  is_un_login: 'menu:config:un_login',
  is_disable: 'menu:config:disable',
}

const otherPermission = {
  edit: 'menu:config:edit',
  del: 'menu:config:del',
}

export { toolPermission, tablePermission, otherPermission }
