const toolPermission = {
  add: 'admin:config:add',
}

const tablePermission = {
  is_super: 'admin:config:isSuper',
  is_disable: 'admin:config:disable',
}

const otherPermission = {
  edit: 'admin:config:edit',
  del: 'admin:config:del',
  password: 'admin:config:password',
}

export { toolPermission, tablePermission, otherPermission }
