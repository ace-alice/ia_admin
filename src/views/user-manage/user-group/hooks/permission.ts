const toolPermission = {
  add: 'user:group:add',
}

const tablePermission = {
  is_disable: 'user:group:disable',
}

const otherPermission = {
  edit: 'user:group:edit',
  del: 'user:group:del',
}

export { toolPermission, tablePermission, otherPermission }
